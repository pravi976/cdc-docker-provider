apiVersion: v1
kind: Service
metadata:
  name: cdc-docker-provider
  labels:
    app: cdc-docker-provider
spec:
  selector:
    app: cdc-docker-provider
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
  type: ClusterIP
